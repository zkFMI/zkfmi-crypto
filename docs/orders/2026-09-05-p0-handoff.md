> **Translator and publication note — 2026-09-05:** This historical work order is preserved as source material. Its requirements for a Japanese README and private visibility were superseded by the user's later decisions to use English documentation and public visibility. Publication is complete: [shukob/zkfmi-crypto](https://github.com/shukob/zkfmi-crypto) is public, its default branch is `main`, and its initial published implementation/documentation HEAD is `25e69708b978b7d1a79258c3318b720f0ead952e`. At the original S1 commit `7bf7a69bb45b0ce37cd7838518d498308a8f7dae`, this file was a byte-for-byte copy of the attachment. This version is a later, user-requested English translation; the original remains in Git history. The commands below retain their historical form.

Before writing the handoff, I will check only the candidate crates' dependency versions (the rand_core / signature version families). This is to prevent Codex from spending excessive time resolving dependencies.

I have finished writing the handoff. It is saved in the scratchpad as `codex_handoff_pqc_p0_20260905.md`. Paste the following into Codex exactly as it appears.

~~~markdown
# Work order: Begin post-quantum P0 independently in a separate Codex thread (new repository: zkfmi-crypto)

Created: 2026-09-05, handed off from a Claude Code session. The time limit for this work is 2.5 hours.

## 0. Mandatory constraints (read first)

- **Another Codex thread is currently editing defmi and oclob**. defmi has uncommitted changes (execution / state / transaction / vm in qomm-avalanche-vm, avalanche.rs and application_settlement.rs in qomm-defmi, and reservation.rs in zkpi-defmi-sdk). oclob is in_progress.
- **Writes prohibited**: All files under `defmi/`, `oclob/`, `zkpi/`, `dekyx/`, `deccp/`, `aethel/`, and `zkfmi-site/`. Changes to Cargo.lock, Dockerfile, docker-compose, and pins are also prohibited. Reading is unrestricted.
- **Writes allowed**: Only the new `/Users/shukob/Research/DeFMI/zkfmi-crypto/` directory (to be created for this task) and the single file `qomm/doc/ja/PQC_MIGRATION_PLAN.md`. Do not touch any other qomm files, code, or Cargo configuration.
- Do not wire this into existing crates (by adding zkfmi-crypto as a dependency) in this task. Do not implement P1 or P2 either (changing transport groups or storing hybrid signatures in zkPI).
- Builds and tests must run **only on softbank-l40s**. Do not run `cargo build` / `cargo test` on the local Mac. Create a new `~/work/zkfmi-crypto/` directory on the remote host, and do not touch other `~/work/*` directories or existing Docker images (another thread may be building on the same host). Use the official `rust:1.97-bookworm` image.
- Rust only. Do not add a Python reference implementation.
- A fallback that accepts only the current signature when post-quantum verification fails is prohibited.
- Do not report performance figures that have not been measured.
- Do not place temporary files in `/tmp`. Keep them in the repository or in `~/work/zkfmi-crypto/`.
- Commit each verified slice. If time runs out, stop at the last verified commit and report without leaving unfinished, uncommitted work.

## 1. Established facts (do not reinvestigate; only report any contradictions you find)

**The plan and the scope of this work**
- The plan is `qomm/doc/ja/PQC_MIGRATION_PLAN.md`. This work corresponds to its P0 phase. The start condition, "completion of the current implementation," has not been met (the OCLOB and DeFMI settlement paths are in_progress). P0 may proceed ahead of that condition because it is designed not to change current behavior.
- None of the 7 repositories contains PQC implementation code (ML-DSA, ML-KEM, or SLH-DSA). BINDING.md 6.3 also explicitly states that "no timing claims are made because there is no implementation."

**Current cryptographic dependencies**
- ed25519-dalek 2.x: DeKYX issuers, DeCCP's AuthoritySignature, Aethel signatures, qomm-defmi's cross-domain Committee / ReceiptSignature / FinalitySigner, qomm-audit's NodeSignature, and the mTLS CA.
- frost-ristretto255 3.0 / frost-core 3.0: zkPI threshold signatures. Used only by qomm-zkpi.
- curve25519-dalek 4.1.3 + bulletproofs 5 + merlin 3: Pedersen commitments, range proofs, and one-of-many proofs.
- openssl 0.10.81: TLS 1.3 mTLS between nodes. Certificate keys are Ed25519. Configuration locations: qomm-transport's `node_service.rs` in defmi (acceptor around line 1431, connector around line 1464), `bin/serve_proof_party.rs`, qomm-defmi's `avalanche.rs` around line 1769, and oclob's `oclob-node/src/network.rs` around lines 448 / 466.
- sha2 and sha3 (voleith in the zkpi harness uses SHAKE128).
- Number of files using these dependencies (src + tests): qomm-transport 34, qomm-defmi 25, qomm-proofs 12, qomm-harness 12, qomm-zk 9, oclob-node 9, qomm-avalanche-vm 8, qomm-zkpi 7, zkpi-defmi-sdk 3, oclob-settlement 3, oclob-demo 3, qomm-audit 3, aethel-provider-sdk 3, dekyx-core 1, deccp-core 1, and 1 each for the other oclob crates. Check the completeness of the inventory (S4) against these counts.

**Structure**
- Common crates are duplicated under `qomm/rust`, `defmi/rust`, and `zkpi/rust`. Number of differing files between qomm and defmi: qomm-transport 0, qomm-zkpi 1, qomm-zk 1, qomm-defmi 9, zkpi-defmi-sdk 4, qomm-mpc 2. Consolidating these copies is a prerequisite for wiring them in during P1.
- The pattern for sharing a separate repository is the same as dekyx-core: "a git URL pinned to a rev." zkfmi-crypto will eventually be used in the same way.
- There are 476 existing versioned domain tags in the form `b"AETHEL:ZKPI:RECEIVABLE:v1"` (uppercase, colon-separated, ending in :vN). The new canonical format must neither collide with nor be confused with these tags.
- Existing key management is in defmi's `qomm-transport/src/key_management.rs` (KeyKind, PublicKeyRecord, PublicSnapshot, PublicManifest, EncryptedKeyStore, and the Ed25519 mTLS CA). DeKYX has issuer key epochs. The new crate's KeyRecord must not replace these; it should provide a common form that they can implement in the future.

**Communication (P1; document this in the plan without implementing it in this task)**
- Both the Ubuntu 24.04 host and Docker bookworm use the OpenSSL 3.0.13 family, which has no post-quantum TLS groups.
- The openssl crate 0.10.81 provides `set_groups_list`. The `vendored` feature of openssl-sys 0.9.117 pulls in openssl-src ^300.2, bundling the latest 300.x (the OpenSSL 3.5 family), so `X25519MLKEM768` can be used without changing the base image. Preventing downgrade requires one line each to fix the groups on the acceptor and connector.
- There is no standard for hybrid signatures on the TLS authentication side. Leave the choice between an ML-DSA certificate alone and dual certificates as a decision to be made.

**Size arithmetic (constants from FIPS 203 / 204 / 205)**

| Scheme | Public key | Signature or ciphertext |
|---|---:|---:|
| ML-KEM-768 | 1,184 B (ek), dk 2,400 B | 1,088 B (ct) |
| ML-DSA-44 | 1,312 B | 2,420 B |
| ML-DSA-65 | 1,952 B | 3,309 B |
| ML-DSA-87 | 2,592 B | 4,627 B |
| SLH-DSA-SHA2-128s | 32 B | 7,856 B |

- 3 ML-DSA-65 signatures for 3-of-7 = 9,927 B; with ML-DSA-44, 7,260 B.
- Current measurements (artifacts in each repo): `package_bytes` is consistently 57,971 B in 23 cases, 57,939 B in 3 cases, and 5,988–7,268 B in smaller cases. `proof_bytes` includes values such as 33,024 / 4,352. Depending on the definition of the current package, the "16KB target / 32KB limit" in Chapter 6 of the plan has already been exceeded, and the additional 10,000 bytes of signatures are not the main cause.

**Candidate crates (crates.io, as of 2026-09-05)**
- RustCrypto: ml-dsa 0.1.1, ml-kem 0.3.2, slh-dsa 0.2.0-rc.5. All are pure Rust, Apache-2.0 OR MIT, with MSRV 1.85.
- integritychain: fips203 0.4.3, fips204 0.4.6, fips205 0.4.1.
- pqcrypto-mldsa 0.1.2, pqcrypto-mlkem 0.1.1 (liboqs-family C).
- aws-lc-rs 1.18.1 (a FIPS-validated path; C build).
- The first choices are RustCrypto's ml-dsa and ml-kem (no C toolchain required; expected to build straightforwardly in Docker). Keep the backend behind a trait boundary so it can be replaced with aws-lc-rs. Record the selection rationale in decisions.jsonl.
- Dependency caveat: RustCrypto's ml-dsa / ml-kem and ed25519-dalek 2.2 are expected to use different major versions of rand_core and signature. Check with `cargo tree -d`; do not try to unify them under a single RNG type or traits from the `signature` crate. Define custom traits in terms of byte sequences, and handle RNGs separately at each backend boundary.
- cargo on softbank-l40s is 1.97.1.

## 2. Deliverables and priority order (if time runs out, retain work in this order)

### S1 Repository scaffold (target: 20 minutes)
- Run `git init` in `/Users/shukob/Research/DeFMI/zkfmi-crypto/`. Add an MIT LICENSE, a Japanese README.md (purpose, boundaries, relationships with other repositories, and the fact that this is not yet wired in), a Cargo workspace, and the `zkfmi-crypto` crate (edition 2021, rust-version 1.85, `#![forbid(unsafe_code)]`).
- Create `.codex/project-memory/{project.toml, facts.jsonl, decisions.jsonl, worklog.jsonl}` in the same format as the other repositories. Set `local_builds_forbidden = true` and `allowed_remote_hosts = ["softbank-l40s"]` in project.toml.
- Save this entire work order as `docs/orders/2026-09-05-p0-handoff.md`.
- Add `remote-gate` to the Makefile: synchronize to `~/work/zkfmi-crypto/` on softbank-l40s, then run `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --release`, in that order, in Docker using `rust:1.97-bookworm`.
- GitHub: First check `gh auth status`. If authentication is valid, run `gh repo create shukob/zkfmi-crypto --private --source . --push`. If invalid, do not run `gh auth login`; proceed locally and record this in the report (the local gh token was invalid as of 2026-09-05). Switching to public visibility is the user's decision.

### S2 Core modules (target: 60 minutes)
All must support serde and use `deny_unknown_fields`, failing closed on unknown suites or versions.
- `suite`: A closed `SuiteId` enum and version. Current: Ed25519, FrostRistretto255, PedersenRistretto255, BulletproofsRistretto255, X25519Tls13, Sha256, Sha512, Shake128. Post-quantum: MlKem768, MlDsa65, MlDsa44, SlhDsaSha2_128s. Hybrid: X25519MlKem768, Ed25519MlDsa65. Give each suite `post_quantum: bool`, a `purpose` (Signature / Kem / Commitment / Proof / Hash / Transport), and public-key and signature-or-ciphertext sizes in bytes; test that they match the FIPS constants above.
- `key`: `ParticipantId` (immutable), `KeyId`, `KeyPurpose` (Quote, SettlementInstruction, KeyRotation, Governance, Transport, Attestation, etc., for purpose separation), `KeyRecord { participant_id, key_id, suite, key_version, public_key, not_before, not_after, revoked_at, rotation_proof, dekyx_binding }`, and `KeyRegistry` (lookup, revocation, validity-period evaluation, and verification of proofs of rotation from an old key to a new key). Rotation proofs are bidirectional: the old key signs the new key, and the new key signs the old key ID. Represent time as u64 seconds, and explicitly specify in code and tests whether intervals are closed or open.
- `canonical`: Deterministic encoding of the canonical signing target from Section 4.2 of the plan: `SigningPreimage { protocol, protocol_version, network_id, deployment_id, contract_id, tx_kind, object_ids, sequence_or_nonce, expires_at, suite, body_hash }`. Use length prefixes, fixed-width integers, and the domain `ZKFMI:CANONICAL:v1` at the beginning. Include at least 3 golden-bytes test cases with fixed hexadecimal (base 16) values. Test that the same body produces different preimages for different purposes.
- `traits`: `Signer`, `Verifier`, `KemEncapsulator`, `KemDecapsulator`, `ProofVerifier`, and `CryptoProvider` (resolve an implementation from a suite; reject unregistered suites). All APIs operate on byte sequences.
- `hybrid::signature`: `HybridSignature { classical, pq }`, using Ed25519 and ML-DSA-65. Verification accepts only when both succeed. Reject failure of either component, a missing component, or swapped component order. Include a purpose tag (KeyPurpose and suite) in the ML-DSA FIPS 204 context.
- `hybrid::kem`: Combine X25519 and ML-KEM-768. Derive the shared secret with HKDF-SHA256, using the domain as the salt and `ss_x25519 || ss_mlkem || ct_x25519 || ct_mlkem || suite_id` as the ikm. Test that the key cannot be recovered from only one component's secret (replacing either component changes the key). For X25519, use curve25519-dalek 4.1.3 or the x25519-dalek 2 family. Pin and document the version.
- `backend`: ed25519-dalek 2.x, ml-dsa 0.1.1, ml-kem 0.3.2. Use zeroize for secret keys.

### S3 Known-answer tests (target: 30 minutes)
- Obtain at least 2 known-answer cases each for ML-KEM-768 encapDecap and ML-DSA-65 sigVer (or deterministic sigGen) from NIST's ACVP-Server repository (gen-val/json-files in usnistgov/ACVP-Server), and save them as JSON under `tests/vectors/`. Record the source URLs and commit hashes in `tests/vectors/SOURCES.md`.
- If they cannot be obtained, report "known-answer tests not performed." Do not call tests bundled with a crate known-answer tests.
- For Ed25519, use 1 test vector from RFC 8032.

### S4 Inventory of cryptographic usage (target: 30 minutes)
- `inventory/crypto_inventory.json`: Scan the 7 repositories (qomm, zkpi, defmi, oclob, dekyx, deccp, aethel) read-only. Each entry is `{ repo, crate, file, primitive, current_suite, purpose, owner, quantum_status, replacement_candidate, migration_phase, notes }`. `quantum_status` must be one of `information_theoretic` / `broken_by_quantum` / `grover_only`. `migration_phase` is P1–P6. File-level granularity is sufficient; create multiple entries when a file uses multiple primitives.
- Target primitives: ed25519_dalek, frost_*, curve25519_dalek (RistrettoPoint / Scalar), bulletproofs, merlin, openssl (TLS, certificates, HMAC), sha2, sha3.
- `scripts/inventory_check.sh`: Compare the set of usage files found by grep with the JSON's (repo, file) set, and exit nonzero if any are missing. Paste the result into the report.
- Check whether the counts match the usage-file counts above. Explain any mismatches.
- Generate `inventory/CRYPTO_INVENTORY.md` from the JSON and include it. Do not maintain a handwritten duplicate.

### S5 Revise the plan (target: 20 minutes)
Make only additions and corrections to `qomm/doc/ja/PQC_MIGRATION_PLAN.md`, preserving the phase structure and basic policy.
- Under "2. Start conditions," add the status as of 2026-09-05 (not met; only P0 proceeds ahead, with the rationale).
- Under "5. P0," add the existence of the zkfmi-crypto repository and the fact that consolidating duplicated crates is a prerequisite for wiring them in during P1.
- In "6. Provisional performance criteria," replace 16KB / 32KB with "to be reset after confirming the definition of the current package_bytes value of 57,971 B," and add the arithmetic above in a table.
- Under "P1," add the OpenSSL facts (the 3.0.13 family, the 3.5 family through vendored, set_groups_list, and authentication-side decisions still to be made).
- Add the candidate-crate list and selection rationale (the same content as decisions.jsonl).
- In qomm, commit only this file. Use the `docs:` prefix.

## 3. Completion conditions (acceptance criteria)
- fmt, clippy (-D warnings), and test --release pass in Docker on softbank-l40s, with the hostname, rustc version, commit hash, and execution time recorded in the report and worklog.
- The test set is complete: FIPS constant agreement, golden preimages, purpose separation, all 4 hybrid-signature quadrants (both valid, classical component only invalid, post-quantum component only invalid, both invalid), replacement of either component in the KEM combiner, fail-closed rejection of unknown suites / versions / revocation / expiration, bidirectional rotation proofs, ACVP known answers, and RFC 8032.
- `scripts/inventory_check.sh` exits with 0.
- The plan diff does not change the phase structure.
- Each slice is committed (and pushed if possible), and worklog.jsonl contains task_id, request, changes, verification, and remaining.

## 4. Report format (mandatory at the end)
- Completed slices and commit hashes (zkfmi-crypto and qomm).
- Verification commands run and their results (pass counts, failure counts, execution host).
- List of paths touched, with a declaration that no other paths were touched.
- Work not performed or left pending, and the reasons.
- Decisions required (public/private repository visibility, adoption of aws-lc-rs, and the TLS authentication approach).

## 5. Prohibited actions (repeated)
- Writing to defmi, oclob, zkpi, dekyx, deccp, aethel, or zkfmi-site, including Cargo.lock.
- Wiring zkfmi-crypto into existing crates, updating pins, or changing Dockerfile and compose.
- Implementing P1 or P2.
- Accepting only the current signature when the post-quantum component fails.
- Reporting unmeasured performance figures. Placing temporary files in `/tmp`. Building or testing on the local Mac. Running `gh auth login`.
~~~

Just 3 additional notes.

- **The gh token was invalid**. Codex cannot create the repository on GitHub, so if you require a push as well, complete `gh auth login` first or create `shukob/zkfmi-crypto` manually. The work can be completed locally even without creating it.
- **The scope is limited to work that will not conflict with Codex running in another thread**. Writes are limited to the new repository and the single plan file in qomm. softbank-l40s CPU resources are shared, so simultaneous builds will only make both slower.
- At acceptance, check the completion conditions against the commit hashes in the report. The arrangement keeps the original work order in the scratchpad and a copy under `docs/orders/` in the new repository.
